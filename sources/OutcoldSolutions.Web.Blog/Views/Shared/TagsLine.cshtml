@using OutcoldSolutions.Web.Blog.Models;
@model IList<Tag>

 @for (int i = 0; i < this.Model.Count; i++)
 {
     var tag = this.Model[i];
     if (i != 0)
     {
<span>, </span>
     }
     
     @Html.ActionLink(tag.Name, "tag", "blog", new { tagId = tag.TagID, id = 1 }, null)
 }